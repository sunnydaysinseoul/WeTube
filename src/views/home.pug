//- URL : /
extends base.pug
include mixins/video.pug 
    //-아래에서 +기호로 사용
block headerContent
    a(href="/videos/upload") Upload Video
block content

        each video in videos
            +videoMxn(video)
        else
            li Sorry, nothing found.