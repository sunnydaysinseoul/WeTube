extends base.pug
block headerContent
    if errorMessage
        span=errorMessage

    if userData
        form(method="POST",action="/join")
            span JOIN VIA GITHUB
            p Username (변경가능):
            input(name="username",type="text" ,value=userData.login, required)
            p Email:
            input(name="email", type="email",value=emailObj.email, required,readonly)
            p Name:
            input(name="name",type="text",required)
            p Password:
            input(name="password",type="password",required)
            input(name="password2",placeholder="비밀번호확인",type="password",required)
            input(type="submit",value="회원가입")    
    else
        form(method="POST")
            p Username:
            input(name="username",type="text" ,required)
            p Email:
            input(name="email", type="email", required)
            p Name:
            input(name="name",type="text",required)
            p Password:
            input(name="password",type="password",required)
            input(name="password2",placeholder="비밀번호확인",type="password",required)
            input(type="submit",value="회원가입")

    hr
    div
        span Already have an account?
        a(href="/login") Log in &rarr;