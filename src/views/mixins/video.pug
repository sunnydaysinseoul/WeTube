mixin videoMxn(video)
    div
        h4
            a(href=`/videos/${video.vId}`)=video.title
        ul
            li #{video.rating}/5
            li #{video.comments} comments.
            li Uploaded #{video.createdAt}
            li #{video.views} views.
