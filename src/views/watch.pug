//- URL : /videos/:vId 
extends base.pug

block content
    h3 #{video.views}#{video.views === 1 ? "view" : "views"}
    h4 Video uploaded #{video.createdAt}

    a(href=`${video.vId}/edit`)
        button Edit Video